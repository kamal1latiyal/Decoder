# Core — CUDA / PyTorch (install PyTorch separately via scripts/install.sh)
torch>=2.7.0
torchvision>=0.22.0

# HuggingFace — qwen-tts pins transformers==4.57.3; honour the same pin to avoid resolver fights.
transformers==4.57.3
accelerate>=1.12.0
huggingface_hub>=0.23.0
safetensors>=0.4.0

# Qwen-TTS (official package — handles processor / subtalker / codec / speaker encoder)
qwen-tts>=0.1.1

# Audio
librosa>=0.10
soundfile>=0.12
# torchaudio is installed alongside torch from the CUDA 12.8 wheel index by
# install.sh — DO NOT pin / re-add it here, pip will replace the CUDA wheel
# with a PyPI build that has an ABI mismatch (see OSError: Symbol not found:
# _torch_library_impl on macOS local testing).
einops

# JIT kernel compilation
ninja>=1.11.0

# FastAPI server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0
uvloop>=0.20.0  # ~2-4x faster asyncio loop (Linux/macOS)

# Pipecat voice pipeline
pipecat-ai[websocket,deepgram,anthropic,silero]>=0.0.60

# Demo + benchmarking
numpy>=1.26.0
sounddevice>=0.4.6
tabulate>=0.9.0
